get:
  responses:
    '200':
      summary: List of databases accessible by the user.
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/Database.yaml
