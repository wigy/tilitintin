
BUGS
====


FUTURE FEATURES
===============

0.9.1
[X] Change documents so that they have correct date.
[X] Compare and verify old reports.
[X] Changing the language from menu.
[X] App.js: Use `shouldComponentUpdate` in an `observer` based component breaks the behavior of `observer` and might lead to unexpected results. Manually implementing `sCU` should not be needed when using mobx-react.

1.0
[ ] Rewrite away current TransactionModel and use directly list of EntryModels.
[ ] Logo with icon.
[ ] Scan all TODOs and make fix.

2.0
[ ] Tags management.
[ ] Material UI.
[ ] Switching between period and document tools: icon is not updated (fa-icon problem).

3.0
[ ] Import management in UI.


CREATE TABLE `tags` (`id` integer not null primary key autoincrement, `tag` varchar(16) not null, `name` varchar(256) null, `mime` varchar(256) null, `picture` blob null, `type` varchar(16) null, `order` integer);
