description: User has not been autehnticated.
content:
  application/json:
    schema:
      properties:
        message:
          description: Error message.
          type: string
          example: Unauthorized.
      required:
        - message
