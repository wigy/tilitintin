description: Invalid payload in the request.
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: string
          example: Invalid parameters for request.
