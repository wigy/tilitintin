description: User is not authenticated.
content:
  application/json:
    schema:
      properties:
        message:
          description: Error message.
          type: string
          example: Access denied.
      required:
        - message
